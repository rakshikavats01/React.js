import Rv from "./rv";
function App() {
  return (
    <> 
    <Rv/>
    <h1>just checking</h1>
    </>
    //fragment:-we can't return more than one thing in a function so 
    //we encapsulate everything inside a div
    //these empty <></>:-are also represent div only
  );
}

export default App;
